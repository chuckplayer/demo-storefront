


Fix the cart total in client/src/stores/cart.ts. 
The total computed property is hardcoded to return 0. 
It should calculate the sum of (item.product.price * item.quantity) 
across all items in the cart, rounded to 2 decimal places using 
Math.round. The CartItem type in types/index.ts already has 
product.price and quantity.



/add-ui-badge components/ProductCard.vue "product.stock_quantity <= 5" "Low Stock" "#f59e0b"


Update the product info page to show the low stock warning.


In client/src/components/CartSidebar.vue, update 
the "Checkout" button so that clicking it closes the cart 
sidebar (cartStore.toggleCart()) and navigates to /checkout 
using Vue Router's useRouter(). Add the /checkout route to 
client/src/router/index.ts pointing to a new view file 
client/src/views/CheckoutView.vue — create that file as a 
minimal placeholder that just renders <div>Checkout coming soon</div> 
for now. The router imports are already established in index.ts 
as a pattern to follow.



/scaffold Add a complete checkout and payment processing feature.

New route /checkout with CheckoutView.vue:
- Customer name, email, shipping address (street, city, state, zip)
- Card type toggle: Visa or Mastercard
  - Visa auto-fills card number: 4111 1111 1111 1111
  - Mastercard auto-fills: 5500 0000 0000 0004
  - Expiry pre-filled 12/27, CVV pre-filled 123 (display only)
- Place Order button submits to POST /api/orders
- On 402 decline: show inline error with gateway message, 
  keep form populated, do not clear card pre-fill
- On success: call cartStore.clearCart(), navigate to /orders/:id

Update POST /api/orders in server/src/routes/orders.ts:
- Accept customer_name and shipping_address in request body
- Simulate payment: 70% approve, 30% decline using Math.random()
- On approval: generate order_number as ORD-{timestamp}, 
  persist with status "confirmed", return 201 with order data
- On decline: return HTTP 402 with a realistic gateway message,
  vary randomly among: "Insufficient funds", 
  "Card issuer declined the transaction", 
  "Do not honor", "Transaction limit exceeded"
- Do not persist declined orders

Update server/src/db.ts orders table to add columns:
- customer_name TEXT
- shipping_address TEXT  
- order_number TEXT
Use ALTER TABLE ADD COLUMN IF NOT EXISTS for each — 
the table already exists and must not be dropped.

Update OrderConfirmationView.vue:
- Fetch from GET /api/orders/:id on mount using route.params.id
- Display order_number, line items with name and quantity, 
  and calculated total
- Replace all placeholder comment text with real data

Wire CartSidebar.vue checkout button to close the cart 
and navigate to /checkout.
Add /checkout route to router/index.ts pointing to CheckoutView.vue.